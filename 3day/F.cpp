#include <iostream>
#include <set>
#include <map>

using namespace std;

int main() {
    int times, qKey, qDir;

    set<int> set1;
    map<int, set<int>> dir;
    int qtKey;
    int val;
    for (int i = 0; i < qDir; ++i) {
        cin >> qtKey;
        for (int j = 0; j < qtKey; ++j) {
            cin >> val;
            dir.in
        }
        set1.insert(1);
    }
    return 0;
}