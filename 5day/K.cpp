#include <iostream>

using namespace std;

int main() {
    int a,b;
    scanf(" %d %d", &a ,&b);
    int vv[a];
    int ini,fim;
    char c;
    for (int i = 0; i <b ; i++) {
        scanf("%c %d %d",&c,&ini,&fim);
        if(c == 'C'){

        } else{


        }
    }
    return 0;
}